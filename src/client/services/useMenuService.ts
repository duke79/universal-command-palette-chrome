import * as React from 'react';

const userServiceNavMenu = () => {
  // const partsMap = getPartsMap(permissionsModule, type);
  // const data = getSideMenuList
  //   .reduce(
  //     (acc, section) => {
  //       const isOnRoot = !sidebarSectionsToShow[section.id] || section.id === 'root';
  //       const options = section.options.map((option) => {
  //         const text = `${partsMap[option]?.rootTitle || partsMap[option]?.sectionTitle}`;
  //         const ret = {
  //           text,
  //           item: partsMap[option],
  //           isOnRoot,
  //           section,
  //         };
  //         return ret;
  //       });
  //       acc = [...acc, ...options];
  //       return acc;
  //     },
  //     [] as Option[],
  //   )
  //   .filter(
  //     ({ text }) =>
  //       !query ||
  //       (text?.toLowerCase && text?.toLowerCase())?.includes(
  //         query?.toLowerCase && query?.toLowerCase()
  //       )
  //   )
  //   .filter(({ item }) => !!item);
  // return data;
};

// const navigateTo = (highlightedIdx) => {
//   let newIdx = highlightedIdx;
//   if(highlightedIdx < 0) newIdx = 0;
//   if(newIdx > listOptions.length - 1) newIdx = listOptions.length - 1;
//   setState({
//     scrollOffset: getScroll(newIdx).scrollTo,
//     highlightedIdx: newIdx,
//   });
// };
